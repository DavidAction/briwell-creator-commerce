"""Brand Partner Hub pipeline (briefing 0.0.19 plan).

Five independent modules, Akeneo-SDM style: extraction (AI, dry-run gated),
ingredient normalization (INCI dictionary), draft validation (Korean
operator/partner-facing reports), regulatory screening (signals only — never
legal advice), and completeness scoring (Salsify-style readiness gauge).
"""
